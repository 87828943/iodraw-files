```mermaid
graph TB
    subgraph 数据层
        DB[(MySQL)] -->|Binlog| Canal
    end
    subgraph 处理层
        Canal -->|Kafka| Parser[解析]
        Parser -->|Kafka| Engine[上送]
        Engine -->|gRPC| Accounting[记账服务]
    end
    subgraph 监控层
        Accounting -->|日志| ELK
        Accounting -->|Metrics| Prometheus
        Prometheus -->|报警| AlertManager
    end
```