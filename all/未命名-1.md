```mermaid
graph TB
    subgraph 数据层
        DB[(MySQL)] -->|Binlog| Canal
    end
    subgraph 处理层
        Canal -->|Kafka| Parser[解析服务]
        Parser -->|serv| Engine[规则引擎服务]
        Engine -->|gRPC| Accounting[记账服务]
    end
    subgraph 监控层
        Accounting -->|日志| ELK
        Accounting -->|Metrics| Prometheus
        Prometheus -->|报警| AlertManager
    end
```